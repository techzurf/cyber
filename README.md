Converted Vite + React project
- Assets copied to /public (css, js, images, webfonts) to preserve original paths.
- Pages generated from HTML files and render the original <body> content using dangerouslySetInnerHTML.
- 'Mirrored from' occurrences were removed from HTML before embedding.
- To run:
  1. npm install
  2. npm run dev
